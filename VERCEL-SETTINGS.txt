VERCEL DEPLOYMENT SETTINGS

Set Vercel Root Directory to:
frontend

Framework Preset:
Vite

Install Command:
npm install

Build Command:
npm run build

Output Directory:
dist

Do NOT use:
cd frontend && npm install

The frontend/vercel.json is already included in this ZIP.
